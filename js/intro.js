// build more interactive website
// almost identical with C, but is a language that browser can interpret and run
// Document Obejct Model(DOM), JS can modify DOM structure


// do not need to specify the type of the variable, use let introduce new variables
let counter = 0;

// function
function cough(n)
{
    print(cough) * n
}

